# dotfiles

    git clone git@github.com:timbleck/dotfiles.git $HOME/.dotfiles
    cd $HOME/.dotfiles
    rake setup

Local settings can be configured in ~/.bash_locale which will be sourced at the end.
